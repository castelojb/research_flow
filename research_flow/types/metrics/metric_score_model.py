from pydantic import BaseModel


class MetricScoreModel(BaseModel):
    """
    Represents a model for storing and manipulating metric scores.

    This model serves as a base structure for metric scores and can be extended to include additional attributes and methods as needed.

    """

    pass
